<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game">

    <application
        android:name=".Application.GameApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Human.HumanCustomActivity"></activity>
        <activity android:name=".Leaderboard.LeaderboardActivity" />
        <activity android:name=".Human.HumanActivity" />
        <activity android:name=".Dashboard.IntroActivity" />
        <activity android:name=".Fight.MainActivity" />
        <activity android:name=".Trivia.Trivia" />
        <activity android:name=".Fight.DisplayGame" />
        <activity android:name=".Trivia.QuizActivity" />
        <activity android:name=".Signup.SignUpActivity" />
        <activity android:name=".Login.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>