package com.example.game.Trivia;

interface QuizInterface {
    /**
     * Update the timer's string
     * @param time current time's string representation
     */
    public void updateTimer(String time);
}
